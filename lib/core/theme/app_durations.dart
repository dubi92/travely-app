class AppDurations {
  static const Duration short = Duration(milliseconds: 150);
  static const Duration medium = Duration(milliseconds: 200); // 200ms
  static const Duration long = Duration(milliseconds: 300); // 300ms
}
